<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "task menu", tasks_path %>
<%= link_to "Edit task", edit_task_path(@task) %>
<%= link_to "Delete task", task_delete_path(@task), method: :delete, data: { confirm:"Are you sure?" } %>
